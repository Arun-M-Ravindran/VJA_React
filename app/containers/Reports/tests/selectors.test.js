// import { fromJS } from 'immutable';
// import { makeSelectReportsDomain } from '../selectors';

// const selector = makeSelectReportsDomain();

describe('makeSelectReportsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
