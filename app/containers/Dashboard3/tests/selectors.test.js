// import { fromJS } from 'immutable';
// import { makeSelectDashboard3Domain } from '../selectors';

// const selector = makeSelectDashboard3Domain();

describe('makeSelectDashboard3Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
