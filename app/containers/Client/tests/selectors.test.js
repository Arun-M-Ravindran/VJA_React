// import { fromJS } from 'immutable';
// import { makeSelectClientDomain } from '../selectors';

// const selector = makeSelectClientDomain();

describe('makeSelectClientDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
