// import { fromJS } from 'immutable';
// import { makeSelectTasksummaryDomain } from '../selectors';

// const selector = makeSelectTasksummaryDomain();

describe('makeSelectTasksummaryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
