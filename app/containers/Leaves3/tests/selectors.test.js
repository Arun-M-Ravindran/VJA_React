// import { fromJS } from 'immutable';
// import { makeSelectLeaves3Domain } from '../selectors';

// const selector = makeSelectLeaves3Domain();

describe('makeSelectLeaves3Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
