// import { fromJS } from 'immutable';
// import { makeSelectTask2Domain } from '../selectors';

// const selector = makeSelectTask2Domain();

describe('makeSelectTask2Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
