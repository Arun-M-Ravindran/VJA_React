// import { fromJS } from 'immutable';
// import { makeSelectTasksDomain } from '../selectors';

// const selector = makeSelectTasksDomain();

describe('makeSelectTasksDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
