// import { fromJS } from 'immutable';
// import { makeSelectDashboard2Domain } from '../selectors';

// const selector = makeSelectDashboard2Domain();

describe('makeSelectDashboard2Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
