// import { fromJS } from 'immutable';
// import { makeSelectTask3Domain } from '../selectors';

// const selector = makeSelectTask3Domain();

describe('makeSelectTask3Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
