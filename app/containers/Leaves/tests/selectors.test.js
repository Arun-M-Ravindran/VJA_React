// import { fromJS } from 'immutable';
// import { makeSelectLeavesDomain } from '../selectors';

// const selector = makeSelectLeavesDomain();

describe('makeSelectLeavesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
