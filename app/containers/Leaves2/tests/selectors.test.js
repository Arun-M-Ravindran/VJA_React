// import { fromJS } from 'immutable';
// import { makeSelectLeaves2Domain } from '../selectors';

// const selector = makeSelectLeaves2Domain();

describe('makeSelectLeaves2Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
