// import { fromJS } from 'immutable';
// import { makeSelectArchivedDomain } from '../selectors';

// const selector = makeSelectArchivedDomain();

describe('makeSelectArchivedDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
